//set event listeners

//set var for num of clicks pl1/pl2

//set function to move plr positions, and add to #clicks

//function to determine win @threshold (determine #clicks needed )
var isWin = function (){
	if (pl1Clicks === 10) {
		alert("Player 1 Wins!!");
	} else if (pl2Clicks == 10) {
		alert("player 2 Wins!!");
	} else{
		return;
	}
	// code for refreshing page
}